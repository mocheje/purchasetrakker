<span class="approve_request">
  <%= form_tag(approve_admin_request_path(@request), :id => "approve_request", :class => "approval") do %>
    <%= submit_tag "Approve", :class => 'bbtn approve' %>
  <% end %>
</span>
